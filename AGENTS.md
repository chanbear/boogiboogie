# AGENTS.md

## 프로젝트 작업 규칙

1. 먼저 `PROJECT_BRIEF.md`와 작업 저장소의 `CLAUDE.md`를 읽는다.
2. 팀 확인 없이 큰 범위를 한 번에 바꾸지 않는다 — 특히 발표 직전에는 더 신중하게.
3. 기능 하나를 수정·검증한 뒤 다음 기능으로 넘어간다.
4. HTML, CSS, JavaScript를 기본으로 사용한다.
5. 로컬 개발은 `server/`에서 `npm start`로 실행한다(Live Server 대신 실제 Express 서버).
6. 실제 서버와 데이터베이스(Express + `node:sqlite`)를 의도적으로 사용한다 — 프론트엔드 시연용 목업이 아니라 실제로 저장·조회되는 백엔드가 있다.
7. 참가자별 데이터가 섞이지 않도록 기기 ID(`X-Device-Id`) 기준으로 분리되어 있다는 전제를 항상 지킨다.
8. 실제로 동작하지 않는 기능(예: 실제 접수 완료)을 완료된 것처럼 UI에서 속이지 않는다 — 방문 접수형 유형은 "방문 준비 필요"로 정직하게 표시한다.
9. 없는 기관 API나 외부 서비스를 작동하는 것처럼 표현하지 않는다.
10. 샘플 데이터를 임의로 만들어 실제 공공데이터처럼 표현하지 않는다 — 출처를 확인한 데이터만 사용한다.
11. 한 번에 기능 하나를 수정한다.
12. 변경 후 기존 기능이 유지되는지 확인한다(`node --check`로 문법, 서버 API 직접 호출로 동작 확인).
13. 브라우저 콘솔 오류를 확인한다.
14. 완료 조건을 기준으로 테스트한다.
15. 확인하지 못한 기능은 완료됐다고 보고하지 않는다.
16. 팀이 실행 결과를 보고 다음 수정을 결정하도록 한다.
