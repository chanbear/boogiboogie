# 부기부기 (Boogie Boogie) — 안산시 노인일자리 신청·배분 지원 웹앱

가제 "부기부기". 안산시 지역 문제 해결 해커톤/공모전 출품작.

## 문제 정의

안산시 65세 이상 노인 중 취업(근로)을 희망하는 비율은 33.6%(2023 노인실태조사)로 추정 약 33,600명에 달하지만, 실제 공공 노인일자리는 6,317자리(전체 노인의 6.3%)뿐이라 취업 희망자의 약 81%가 일자리를 얻지 못한다. 게다가 신청 절차가 온라인 중심이라 디지털 기기에 익숙하지 않은 고령층은 신청 단계에서부터 배제되며, 정작 가장 필요한 사람(저소득·독거·고령)이 아니라 디지털 접근성이 높은 사람이 자리를 먼저 채가는 구조적 불공정이 있다.

방치하면 일자리에서 배제된 노인의 건강 악화 → 의료비 지출 증가(노인 1인당 진료비는 전체 국민 평균의 2.5배, 2019~2023 노인진료비 1.4배 증가)로 이어진다는 것이 선행연구(임재영 2008, 이석원 2009, 한국보건사회연구원 2017)로 확인되어 있다.

## 앱이 하는 일 (해결 방안)

"공급을 늘리는 앱"이 아니라 **"노인일자리에 대한 노인들의 접근성을 높이는 앱"**. 초점은 배분의 공정성(누가 붙는가)이 아니라 접근의 공정성(누구나 신청할 수 있는가)이다.

1. **신청 접근성 지원**: 복잡한 온라인 노인일자리 신청서를 AI가 대화형으로 안내하며 대신 작성. 디지털 기기 사용이 어려운 노인도 쉬운 질답으로 신청 완료.
2. **신청 이력 조회**: 내가 신청한 내역과 현재 상태를 쉽게 확인.

> 우선순위 판단/추천 로직은 넣지 않는다 (팀 결정, 2026.7.24). "누가 더 필요한지 AI가 판단한다"는 기능은 스코프 밖.

## 차별점 (반드시 지킬 것)

기존 노인일자리정보시스템(seniwork.or.kr)·시니어클럽은 "정보 게시 + 신청 접수"에 머물러 있어, 디지털 기기 사용이 어려운 노인은 신청 단계에서부터 배제된다. 이 앱은 그 접근성 공백을 메운다 — **핵심 가치는 "신청 장벽 해소"**이지, 일자리 자체를 만들어내거나 배분을 재조정하는 서비스가 아니다. 기능을 추가할 때 이 프레임을 벗어나지 않는지 항상 확인할 것.

**주의 (발표 시 반박 대비)**: "접근성만 높이면 자리는 그대론데 경쟁만 세지는 거 아니냐"는 반박이 나올 수 있음. 방어 논리는 "우리 목표는 결과(누가 선정되는가)가 아니라 기회(누구나 동등하게 신청할 수 있는가)다 — 디지털 장벽 때문에 신청 자체를 못 하는 상황 자체가 별개의 불공정이며, 경쟁률 문제와 무관하게 해결할 가치가 있다"는 것.

## 화면 구성 (2026.7.25 전면 개정 — 카톡 전달 visily 목업 반영, 이전 7.24 홈 레이아웃 대체)

카카오톡으로 전달받은 `visily-multiscreens.pdf` 목업 구조를 새 기준으로 채택. `design/mockup-v1.html`에 반영 완료.

**첫 접속**: 이름·성별·나이 입력(이름 입력 필드 신규 추가) → 입력 후 앱 튜토리얼 자동 시작

**홈 화면** (7.24 "세로 3분할 메뉴" 구조를 대체)
- 상단: "부기부기" 타이틀 바 → 중앙 정렬된 아바타(탭하면 프로필 사진 설정, 카메라 배지 표시) → 인사말("안녕하세요, 김정순님") → 나이·성별
- 대형 카드 2개: "일자리 구하기"(강조색) / "신청 이력"
- **공공데이터 확인하기** 배너를 홈에 직접 노출 (7.24엔 설정으로 옮겼었으나 재변경 — 심사기준 2번 데이터 근거 대응용, 항상 눈에 띄어야 함)
- 하단 "도움이 필요하신가요?" 섹션: "설정 변경" / "고객센터" 2칸 카드 (고객센터는 상담 채널 미정으로 현재 클릭 동작 없음)

**하위 화면**
- 일자리 구하기 → 카테고리 4개(공익활동형/사회서비스형/시장형사업단/취업알선형, 세로 리스트, 뱃지·설명·"자세히 보기" 링크 포함) → 카테고리별 일자리 목록
  - 일자리 목록 상단 "내 위치 주변 일자리 찾아보기" 버튼 → 지금 목록에 있는 공고들의 위치를 지도에 핀으로 표시
  - 일자리 카드를 누르면 신규 **일자리 상세 정보** 화면으로 이동(근무시간·장소·급여·모집인원·신청기간·참여자격을 정리해서 보여줌)
- 신청 이력 조회하기 → 내 신청 상태(대기/선정 등 단순 배지 표시)
- 설정 → 화면 및 소리(글씨 크기/음성 안내/알림 설정) / 서비스 및 정보(언어) / 지원 및 보안(개인정보 처리방침/앱 정보 및 약관)/ 로그아웃. **공공데이터 확인하기는 설정에서 빠지고 홈으로 이동함**

**중요 사실 발견 (2026.7.24, 안산시청 페이지 실제 확인): 노인일자리 신청은 원래 온라인 접수가 아예 없다**
- 안산시 공식 안내: "수행기관에 참여 신청서·개인정보동의서·자격증 사본을 **방문 접수 제출**" — 온라인 신청 자체가 존재하지 않음
- 즉 문제는 "온라인 신청이 복잡해서 디지털 취약 노인이 배제된다"가 아니라, **애초에 온라인 접수가 없어서 거동이 불편하거나 교통수단이 없는 노인 전체가 불리하다**는 것 — 문제 정의를 이렇게 갱신할 것
- 그래서 "이 앱에서 신청까지 끝낸다"는 건 불가능함 (실제 접수 자체가 오프라인이므로). 앱의 현실적 역할은 방문 전 **서류 준비(체크리스트·양식 작성·인쇄)**를 도와주는 것으로 재정의됨

**재검증 (2026.7.25, 실제 브라우저로 재확인): "전부 방문접수만 가능하다"는 유형별로 다르다 — 취업알선형은 예외**
- **공익활동형·사회서비스형·시장형사업단** (안산시니어클럽 등 공공형): 안산시청 페이지의 "방문 접수" 결론 그대로 유지. 노인일자리 통합 포털(**노인일자리여기**, seniorro.or.kr, 한국노인인력개발원 운영)에서 "안산시"로 검색해도 이 유형 공고는 **0건** — 애초에 이 온라인 시스템 자체에 이 유형이 올라오지 않음
- **취업알선형** (현장실습훈련지원사업 — 민간기업 채용연계): 위 seniorro.or.kr에서 안산시 검색 시 실제로 "접수중"인 안산 소재 공고가 존재했고(예: 주식회사 유니밸류, (주)휴비즈코리아), 상세 페이지의 "접수하기" 버튼을 누르면 **로그인 없이 이름·성별·생년월일·전화번호·주소만 입력하는 실제 온라인 신청서**가 뜸(실제 제출은 하지 않고 폼 존재만 확인). 즉 이 유형만큼은 **진짜 온라인 신청이 이미 가능**함
- 결론: "노인일자리 = 전부 방문접수"는 부정확한 일반화였다. **4개 카테고리 중 3개(공익활동형/사회서비스형/시장형사업단)는 방문접수 결론 유지, 취업알선형 1개는 이미 실제 온라인 신청 시스템이 존재**함 — 발표 시 이 구분을 명확히 할 것

**"신청하러 가기" 동작 (2026.7.24 결정, 2026.7.25 재검증 반영)**
- 일자리 목록 카드의 "신청하러 가기"는 지금은 **실제 안산시 노인일자리 페이지**(ansan.go.kr, `cntnts_id=C0001530`)로 새 탭 연결됨. 디자인 리뷰·사용자 테스트에서 가짜 완료 화면 대신 진짜 내용(위 발견사실 포함)을 보여주기 위한 임시 조치
- **구현 완료 (2026.7.25)**: 취업알선형 카테고리는 ansan.go.kr 대신 **seniorro.or.kr**(노인일자리여기 메인 검색 페이지)로 연결되도록 변경함 — `server/db.js`의 `rowToJob`이 `cat`에 따라 `applyUrl`을 계산해서 내려주고(`job` → seniorro.or.kr, 나머지 → ansan.go.kr), `public/index.html`과 `design/mockup-v1.html` 양쪽 모두 이 값을 사용하도록 수정. 브라우저로 두 파일 모두 카테고리별 링크가 올바르게 갈라지는 것을 확인함. 나머지 3개 카테고리는 여전히 안산시 페이지 연결 유지
- 앱 내 "AI 신청 도우미"(대화형 작성 → 제출 → 완료 확인) 화면은 그대로 만들어뒀지만, 지금은 일자리 목록에서 연결되지 않고 별도 프리뷰로만 존재 — **실제 앱 빌드 단계에서는 "서류 준비 도우미"로 목적을 바꿔서 재연결**할 것

**"이 앱에서 직접 신청 가능한가"에 대한 결론 (2026.7.25 재검증 반영, 절대 흐리지 않기)**
- 가능한 것: (1) 앱 안에서 AI와 대화하며 신청서·동의서 내용을 완성하고, 방문 시 챙길 서류 체크리스트까지 정리하는 것. (2) **취업알선형**은 seniorro.or.kr 실제 접수 폼으로 연결되어 온라인 신청 자체가 진짜로 가능 (구현 완료, 2026.7.25)
- 불가능한 것: 공익활동형·사회서비스형·시장형사업단은 실제 접수 자체가 방문 접수만 가능하므로 앱에서 "제출"이 곧 실제 신청 완료를 의미하지 않음
- 추천 확장 방식: 공익활동형 등 3개 카테고리는 "제출하기"가 신청서 작성 완료를 의미하고 그 다음 "방문 준비물 안내"로 이어지는 정직한 흐름 유지. 취업알선형은 seniorro.or.kr 연동으로 실제 온라인 신청까지 완결 가능. 실제 접수가 앱에서 끝난 것처럼 과장해서 발표하지 않는다(단, 취업알선형은 실제로 끝나므로 예외)

**디자인 톤**: Apple 스타일 (미니멀, 여백, 둥근 카드, 시스템 폰트)

## 백엔드 구현 (2026.7.25 추가)

`server/` + `public/`에 실제로 동작하는 풀스택 앱이 생겼다. `design/mockup-v1.html`(localStorage 기반)은 디자인 리뷰용 스위처/노트 패널이 남아있는 **디자인 검토용 산출물로 그대로 보존**하고, `public/index.html`은 그 스위처/노트를 제거하고 fetch로 `server/`의 API를 호출하는 **실제 앱**이다.

**스택**: Node.js + Express + `node:sqlite`(Node 22.5+ 내장 모듈, 별도 네이티브 빌드 불필요 — 처음엔 `better-sqlite3`를 썼으나 Windows에 Visual Studio Build Tools가 없어 네이티브 컴파일이 실패해서 교체함). 단일 데모 사용자 기준(로그인/인증 없음), 팀원 누구나 `cd server && npm install && npm start` 후 `http://localhost:3000` 접속.

**구조**
- `server/db.js` — SQLite 스키마(profile/settings/applications/categories/jobs) + 시드 데이터(카테고리 4개, 일자리 8건) + CRUD 함수
- `server/server.js` — Express 라우트(`/api/state`, `/api/onboard`, `/api/avatar/toggle`, `/api/settings`, `/api/categories`, `/api/jobs`, `/api/jobs/:id`, `/api/applications`, `/api/logout`) + `public/` 정적 서빙
- `public/index.html` — 프론트엔드. 온보딩→튜토리얼→홈→카테고리→일자리 목록→상세→신청→이력, 설정까지 전부 서버 상태로 동작

**의도적으로 비워둔 것**: 다중 사용자/로그인 없음(단일 데모 계정), 고객센터 번호·개인정보처리방침·약관은 플레이스홀더, 신청하기는 여전히 실제 안산시 페이지로 새 탭 연결.

## 모바일 앱 "일자리 알리미" (2026.7.25 추가)

앱 이름을 **일자리 알리미**로 확정하고, `mobile/`에 안드로이드 APK(Capacitor)와 아이폰용 PWA를 만들었다.

**왜 서버 연동판이 아니라 localStorage판을 패키징했는가**: 폰에 설치되는 앱은 노트북에서 띄운 `server/` Express 서버에 네트워크로 접근할 방법이 없다(같은 와이파이가 아니면 아예 불가능). 그래서 `mobile/www/index.html`은 `design/mockup-v1.html`의 **localStorage 기반, 완전 오프라인 동작** 버전을 기반으로 만들었다 — 스위처/노트 패널만 제거하고 앱 이름을 "일자리 알리미"로 바꿈. `server/`+`public/`(백엔드 연동판)은 그대로 유지하고 있으니, 나중에 서버를 실제로 어딘가에 배포(Render/Railway 등)하게 되면 그때 `mobile/www`를 fetch 기반 버전으로 바꿔서 다시 패키징하면 된다.

**안드로이드 (Capacitor, 실제 APK 빌드 완료)**
- 스택: Capacitor 6 + 안드로이드 SDK 커맨드라인 툴(Android Studio 없이 sdkmanager로 platform-tools/platforms;android-34/build-tools;34.0.0만 설치, `C:\Users\OWNER\Android\Sdk`)
- appId `kr.boogiboogie.iljaalrimi`, appName "일자리 알리미"
- 빌드된 디버그 APK: `mobile/dist/일자리알리미.apk` (설치 시 "출처를 알 수 없는 앱 설치 허용" 필요 — 아직 서명·앱스토어 배포는 안 함, 데모/사이드로드용 디버그 빌드)
- 재빌드 방법: `cd mobile/android && ./gradlew.bat assembleDebug` (웹 코드 수정 후에는 `mobile/www`를 고치고 `npx cap sync android`로 반영 필요)

**아이폰 (PWA)**
- `mobile/www/manifest.json` + `mobile/www/sw.js` 추가 — Mac·Xcode 없이 아이폰에서 Safari로 접속 후 "홈 화면에 추가"하면 독립 실행 앱처럼 설치되고 오프라인에서도 열림
- 진짜 네이티브 iOS 앱(앱스토어 배포)이 필요해지면 그때 Mac을 구해서 Capacitor iOS 플랫폼을 추가하면 됨 — 지금은 PWA로 "아이폰에서도 쓸 수 있다"는 요건만 충족

**아이콘**: `mobile/generate-icons.js`로 zlib만 이용해 브랜드색(#007AFF) 단색 PNG를 직접 생성함(디자이너 리소스 없이 최소 요건만 채운 플레이스홀더 — 나중에 실제 로고로 교체할 것)

## 디자인 목업 시스템 구현 상태 (2026.7.25, `design/mockup-v1.html` 전용)

`design/mockup-v1.html`은 정적 디자인 목업이 아니라 **localStorage 기반 상태를 가진 클라이언트 사이드 프로토타입**이다(위 백엔드 연동판인 `public/index.html`과는 별개로, 디자인 리뷰/공유용으로 계속 유지).

**실제로 동작하는 것**
- 온보딩에서 입력한 이름·성별·나이가 `localStorage`에 저장되고 홈 화면에 그대로 반영됨. 이름 미입력 시 "다음 단계로 이동" 버튼 비활성화
- 카테고리 4개가 각각 실제로 다른 일자리 데이터(총 8건, `JOBS` 배열)를 보여줌 — 이전엔 카테고리 무관하게 동일한 3건이 노출되던 버그였음
- 일자리 카드 클릭 → 해당 공고의 실제 데이터로 채워진 **일자리 상세 정보** 화면 이동
- "신청하기" 클릭 시 신청 이력에 실제로 기록되어 **신청 이력 조회하기** 화면에 누적 표시 (중복 신청 방지)
- 검색창(제목·기관명 실시간 필터) · 그리드/목록 보기 전환이 실제로 동작
- 설정(글씨 크기·음성 안내·알림·언어)이 `localStorage`에 저장되어 새로고침해도 유지됨
- 로그아웃 시 저장된 모든 상태를 지우고 온보딩 화면으로 완전히 리셋
- 재방문 시(온보딩 완료 이력이 있으면) 온보딩을 건너뛰고 바로 홈 화면에서 시작 — 실제 앱의 "로그인 유지"에 해당하는 동작을 시뮬레이션

**의도적으로 비워둔 것 (실제 앱 단계에서 채울 부분)**
- 데이터는 브라우저 로컬 저장소에만 있음 — 기기를 바꾸거나 캐시를 지우면 사라짐. 실제 앱은 서버/DB 저장 필요
- 로그인/인증 없음 — 로그아웃은 로컬 상태 초기화 시뮬레이션일 뿐, 실제 계정 시스템 아님
- 고객센터 전화번호는 예시 값, 개인정보 처리방침·약관은 플레이스홀더 문구
- "일자리 상세"와 "일자리 목록"의 신청하기는 여전히 실제 안산시 페이지(ansan.go.kr)로 새 탭 연결 — 방문 접수만 가능하다는 결론은 유지

## 기능 우선순위 (2026.7.24 재작성 — 이전 버전 전체 대체)

**MUST HAVE**
- 첫 접속 시 이름·성별·나이 입력 → 완료 후 튜토리얼 자동 시작
- 홈 화면: 중앙 정렬 아바타/인사말/나이·성별 + 대형 카드 2개(일자리 구하기·신청 이력) + 홈에 직접 노출된 공공데이터 배너 + 도움말 섹션(설정 변경·고객센터)
- 일자리 구하기 → 세로 카테고리 4개(뱃지·설명·자세히 보기 링크) → 카테고리별 일자리 목록
- 일자리 목록 상단 "내 위치 주변 일자리 찾아보기" → 목록에 있는 공고들의 위치를 지도 핀으로 표시
- 일자리 카드 → **일자리 상세 정보** 화면(근무시간·장소·급여·모집인원·신청기간·참여자격)
- 일자리 목록/상세의 "신청하기" → (디자인/테스트 단계 한정) 실제 안산시 노인일자리 페이지로 새 탭 연결
- 신청 이력 조회하기 → 내 신청 상태(단순 배지)
- 홈 "공공데이터 확인하기" → 통계 카드·막대그래프·표로 구성된 별도 데이터 분석 화면
- Apple 스타일 디자인 (라이트/다크 모두 대응)

**SHOULD HAVE**
- 설정: 글씨 크기 조절
- 설정: 음성 안내 켜기/끄기

**COULD HAVE**
- 설정: 언어 변경
- 실제 공공기관 API 연동 (인증·안정성 리스크 있음 — 시간 되면 시도, 안 되면 "완성된 신청서를 수행기관에 자동 전달"하는 시뮬레이션으로 대체)

**WON'T HAVE**
- AI 기반 우선순위 판단·추천·매칭 (접근성에만 집중한다는 방향 유지)
- 실시간 챗봇 상담(AI 신청 도우미는 신청서 작성 전용이지, 자유 상담 기능이 아님)
- 실제 공공기관 접수 시스템과의 실시간 API 연동(위 "직접 신청 가능한가" 참고) — 단, 취업알선형을 seniorro.or.kr의 기존 공개 접수 폼으로 **새 탭 연결**하는 것은 API 연동이 아니라 지금 ansan.go.kr에 하는 것과 동일한 링크 패턴이라 여기 해당 안 됨(2026.7.25 재검증 참고)

## 팀/제약 조건

- 팀 규모: 2~4명 (개발 가능 인원 포함), 해커톤 스코프
- 과거 팀 피드백: "기능이 너무 많다"는 지적을 받은 적 있음 → **기능을 늘리기보다 핵심 루프를 완성도 있게 만드는 것을 최우선**으로 한다. 언어 변경·지도 시각화 등 COULD HAVE 항목은 MUST HAVE(첫 접속~홈~카테고리)가 끝난 뒤에만 손댄다.
- 실시간 외부 API 연동은 인증·안정성 리스크가 크므로, 데모용 데이터는 공공데이터포털에서 미리 받은 정적 시드 데이터로 구성하는 것을 기본으로 한다.

## 데이터 소스 (확보됨, `data/` 폴더에 원본 보관 예정)

- 안산시 노인및독거노인 인구수 (2025.8.31 기준, 동별) — 총 노인 99,829명, 독거노인 28,636명(28.7%)
- 노인일자리현황 (2026.3 기준, 경기도 31개 시군) — 안산 6,317자리
- 2023년도 노인실태조사 보고서 (보건복지부·한국보건사회연구원) — 취업 희망률 33.6%
- 국민건강보험공단 건강보험 노인인구 현황 (2023.12.31) — 노인 진료비 통계
- 보건복지부 독거노인 수 (시도별, 2024.12.31)

## 심사 기준 (6개) — 기능/문구 결정 시 이 기준에 맞는지 체크

1. 문제 발견 — 지역성, 구체성, 이해당사자 관점, 문제 정의 명확성
2. 데이터 근거 — 공공데이터 선택과 해석의 적합성, 반영의 타당성
3. 토론을 통해 알게 된 문제점 보완 반영 여부
4. 구현 완성도 — AI 활용(프로토타입 작동 여부, 생성형 AI 활용 과정의 주도성과 검증)
5. 지속가능성 — 확장성, 지역사회 실제 적용 가능성, 사회적 가치
6. 발표 — 전달력

## 데이터 주의사항 (과장 금지)

- 안산 노인일자리 비율은 경기도 31개 시군 중 15위, 중위권 — "안산이 경기도 최악"이라는 식으로 과장하지 말 것. 실제로는 "경기도 대도시 전반의 구조적 문제이고 안산도 예외가 아니다"가 정확한 근거 수준.
- 노인일자리 참여 감소(2024→2025, -4.3%)의 정확한 원인 미확정 — 예산 삭감인지 신청자 감소인지 불명확
- 선행연구의 "일자리 참여 → 의료비 절감" 인과관계는 역인과(건강한 사람이 애초에 일자리에 지원) 가능성 배제 안 됨
- 경기도 31개 시군 순위는 취업자수÷고용률로 역산한 추정치(통계청 경제활동인구조사 기반), 공식 인구총조사 수치 아님
